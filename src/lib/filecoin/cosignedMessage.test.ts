import { CosignedLotusMessage } from './cosignedMessage';
import { expect } from 'chai';

describe('Cosigned Lotus Message', () => {
  describe('validate struct into Cosigned Lotus Message', () => {
    it('it should construct valid Lotus Message', () => {
      expect(true);
    });
  });
});
